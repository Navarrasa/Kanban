import { Columns } from '../Columns/Columns';

export function Board() {
    return(

        <section className="container">
            <h1>My Board</h1>
            <Columns />
        </section>

    );
}